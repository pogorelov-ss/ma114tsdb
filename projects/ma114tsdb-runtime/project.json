{
  "name": "ma114tsdb-runtime",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "projects/ma114tsdb-runtime/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv build",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pytest tests/",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "dependsOn": ["build"],
      "cache": true
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python -m ma114tsdb_runtime",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python -m pylint src/",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "cache": true
    },
    "deploy-infra": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd cdk && cdk deploy --all",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "dependsOn": ["^deploy-infra"]
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "kubectl apply -f k8s/",
        "cwd": "projects/ma114tsdb-runtime"
      },
      "dependsOn": ["build", "deploy-infra"]
    }
  },
  "tags": ["type:application", "scope:runtime"]
}
